# ビルドに要らないものは context へ入れない。
# .git を除くため、Dockerfile では go build -buildvcs=false を指定している。
.git
.gitignore
.github
.claude
.specify
.dockerignore
Dockerfile
compose.yaml
Taskfile.yml
bin/
docs/
specs/
**/node_modules
# SPA の成果物は web ステージから COPY する。手元のビルドを持ち込まない。
web/dist
*.log
.env
.env.*
.DS_Store
